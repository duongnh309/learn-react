import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

Header.propTypes = {
    
};

function index(props) {
    return (
        <div>
           Header 
        </div>
    );
}

export default index;